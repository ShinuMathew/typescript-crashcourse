let drawPoint = (point: {x : number, y : number}) => console.log(point)

// Inline annotation are ways to define a custom type.
/**
 * Here assume we want our drawPoint method to take arguments as a certain type like x and y
 * We can assign the type of point argument as custom {x:umber, y: nnumber} 
 */